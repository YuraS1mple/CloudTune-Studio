<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Список замовлень</title>
  <style>
    /* Стилізація контейнера списку замовлень */
    .orders-container {
        width: 60%;
        margin: 20px auto;
        background-color: #f2f2f2;
        border-radius: 8px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        padding: 20px;
        max-height: 400px; /* Максимальна висота */
        overflow-y: auto; /* Включаємо прокрутку */
    }

    /* Стилізація заголовка "Список замовлень" */
    .orders-container h1 {
        text-align: center;
        margin-bottom: 20px;
    }

    
    .orders-container div {
        padding: 10px;
        border-bottom: 1px solid #ccc;
    }

    .orders-container p {
        margin: 5px 0;
    }

    .scroll-down {
        text-align: center;
        margin-top: 10px;
    }
  </style>
</head>
<body>

<div class="orders-container">
  <h1>Список замовлень</h1>

  <% @orders.each do |order| %>
    <div>
      <!-- Відображення даних про замовлення -->
      <p>Ім'я: <%= order.full_name %></p>
      <p>Email: <%= order.email %></p>
      <p>Номер телефону: <%= order.phone %></p>
      <p>Модель автомобіля: <%= order.car_model %></p>
      <p>Колір автомобіля: <%= order.color %></p>
      <!-- Додайте інші поля для відображення -->
    </div>
  <% end %>

  <div class="scroll-down">
    
  </div>

</div>


<a id="scroll-target"></a>

</body>
</html>
